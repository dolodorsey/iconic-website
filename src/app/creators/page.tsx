import type { Metadata } from 'next';
import { CinematicCreators } from '../_cinematic';
export const metadata:Metadata={title:'Creators',description:'Introduce your creative work to ICONIC. Artists, photographers, filmmakers, hosts, producers and designers.'};
export default CinematicCreators;
