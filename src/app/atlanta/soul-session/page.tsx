import type { Metadata } from "next";
import SoulSessionPage from "../../series/30-plus/page";

export const metadata: Metadata = {
  title: "Soul Session — 30+ Atlanta Concert Series",
  description: "Soul Session is ICONIC's recurring 30+ Atlanta concert series centered on R&B, soul, neo-soul and legacy hip-hop.",
};

export default SoulSessionPage;
