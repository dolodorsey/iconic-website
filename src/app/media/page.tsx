import type { Metadata } from 'next';
import { CinematicMedia } from '../_cinematic';
export const metadata:Metadata={title:'Media & Archive',description:'ICONIC photography, film, campaign creative, press and licensing requests.'};
export default CinematicMedia;
